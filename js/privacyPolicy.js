const privacyPolicy = document.querySelector('.privacyBtn');
const privacyContent = document.querySelector('.privacy');

privacyPolicy.addEventListener('click', () => {
    privacyContent.style.display = 'block';
})
